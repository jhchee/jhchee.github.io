// Placeholder plugin - functionality is commented out
const imgToJsx = () => (tree) => {
  // Image transformation plugin is disabled
  // If needed in the future, this can be re-enabled with proper server-side handling
};

export default imgToJsx;
